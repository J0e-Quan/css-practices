# 01-simple-blog

In the first exercise, you will style a simple blog post using external or internal CSS (if you use internal CSS, just ignore the `styles.css` file.)

Here's what you need to do:

- give `body` a background colour of `aqua`
- give the divs with the classes `first-section` and `ending` a background colour of `blueviolet` and padding of `1rem`
- make the text inside the `first-section` and `ending` divs white

## Outcome image: 

![Picture of the intended result of the exercise](./result.webp)